#include <stdio.h>
/**
 * print_diagonal - prints a diagonal line on the terminal. 
 *
 * Return: void
 */
void print_diagonal(int n)
{
	i=1;
	while (i <= n)
	{
		putchar('\');
		i++;
	}
	putchar('\n');
}
