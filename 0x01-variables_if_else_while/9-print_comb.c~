#include <stdio.h>
/**
 *main - prints numbers in base 16.
 *
 *Return 0.
 */
int main(void)
{
  int number;
  number='0';
  while(number>='0'&&number<='9')
    putchar(number++);
  number='a';
  while(number>='a'&&number<='f')
    putchar(number++);
  putchar('\n');
  return (0);
}
