#include <stdio.h>

/**
 *main - prints alphabet in reverse.
 *
 *Return 0.
 */
int main(void)
{
  
  int letter;

  letter = 'z';
  while(letter >= 'a' && number <= 'z')
    putchar(letter--);
  putchar('\n');
  return (0);
}
